function move(command) {
    // let availableTownsList = document.getElementById('available-towns')
    // let selectedTownsList = document.getElementById('selected-towns')

    // to do
  }


// "right" is received when the  button is clicked – move the selected town from Available towns to Selected towns
// "left" is received when the  button is clicked – move the selected town from Selected towns to Available towns
// "print" – Replace the contents of the div element with ID "output" with a list of the town names of all towns from Selected towns, joined with a semicolon and space ("; ").